public class DALHelper {
	Yogesh44445
	private static final Logger logger1 = LoggerFactory.getLogger(DALHelper.class.getName());	
	private static final Logger logger2 = LoggerFactory.getLogger(DALHelper.class.getName());
	private static final Logger logger3 = LoggerFactory.getLogger(DALHelper.class.getName());
	private static final Logger logger4 = LoggerFactory.getLogger(DALHelper.class.getName());
}
}
